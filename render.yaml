services:
  - type: web
    name: cashier-system
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: POS_ADMIN_PIN
        sync: false
